#!/usr/bin/env nextflow

/*
To run this pipeline during development:
From the MAG_Pipeline/pipeline_test/ folder, run the following command:
    nextflow run ../setup.nf --illumina $path --nanopore $path --pacbio $path --databases $paths
*/

// How to call specified arguments
// How to write to csv
